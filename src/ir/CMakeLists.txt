file(GLOB_RECURSE _IR_SRC ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp ${CMAKE_CURRENT_SOURCE_DIR}/*.cc ${CMAKE_CURRENT_SOURCE_DIR}/*.C)

set(IR_SRC ${_IR_SRC} PARENT_SCOPE)

# add_library(${PROJ_NAME}_IR STATIC ${_IR_SRC})